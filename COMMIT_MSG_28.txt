phase4: _BE/_LE globals, endianness params, c() optional offset

Sixth step of Phase 4 (CLI). Pre-load the "read" include script to
define _BE/_LE global constants, add endianness-aware wrappers for
integer read functions, and fix c() to accept optional offset.

Pre-load "read" include script:
- Defines _BE=true, _LE=false global constants
- Many rules use _BE/_LE without explicitly calling
  includeScript("read")
- Evaluated in load_database() right after _init

Endianness-aware JS wrappers:
- U16/U24/U32/U64 and read_uint16/read_int16/read_uint24/
  read_uint32/read_int32/read_uint64/read_int64 now accept optional
  bigEndian boolean parameter
- Native functions remain 1-arg (LE only); JS wrapper dispatches
  to BE helper functions when bigEndian is true
- BE helpers read bytes via U8 and combine in big-endian order

c() optional offset wrapper:
- X.c("signature") now equivalent to X.c("signature", 0)
- Consistent with compare() wrapper behavior

Bug fixes:
- host.rs: All read_u16/u24/u32/u64 methods now use checked_add
  to prevent integer overflow panic on large offsets
- evaluate_rule_source: Use extract_exception_message for proper
  JS exception type and message (e.g. "TypeError: ...")

New test: scan_jpeg_signature

Corpus scan improvements:
- JPEG: "image: JPEG (1.01) [1x1, YCbCr]"
- WAV: "audio: RIFF container/WAVE file"
- Java Class: "format: Java Class File (.CLASS) (Java SE 8)"

cargo fmt/clippy/test/check-deps all pass (356 tests total).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
