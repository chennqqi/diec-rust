phase3: rule loading conformance tests and public API

Fifth step of Phase 3 (rule compatibility runtime). Add comprehensive
conformance tests for rule loading and host API interaction.

New integration test module tests/conformance.rs (22 tests):

Rule loading:
- meta + detect loads and produces results
- Multiple _setResult calls produce multiple results
- _setLang sets language on last result
- Rule without detect() function fails evaluation

Signature and read:
- Binary.compare matches/doesn't match
- Binary.findSignature returns offset
- Binary.readByte, readWord, U8, U32 aliases
- Binary.getString reads NUL-terminated string

Metadata and scan mode:
- Binary.getSize returns file size
- Binary.calculateEntropy for all-zeros
- Binary.isDeepScan returns false by default

Include and globals:
- includeScript loads helper function
- _getEngineVersion returns "3.10"
- _getOS returns current OS name

Lifecycle:
- Factory creates working runtime
- shutdown() resets state, evaluate fails after
- Cancellation token produces Cancelled error
- Multiple rules in snapshot

API changes:
- RquickjsRuntime::new is now pub
- RquickjsRuntime::register_host_api() public method for
  registering HostApiBridge without accessing private context

cargo fmt/clippy/test/check-deps all pass (328 tests total:
44 diec-core + 98 diec-rules lib + 22 diec-rules conformance +
156 diec-formats + 5 corpus + 3 other).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
