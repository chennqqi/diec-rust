phase4: register all format globals, add DEX/PYC detection

Eleventh step of Phase 4 (CLI). Register all format-specific global
objects (RAR, DEX, PYC, APK, Archive, etc.) as aliases to Binary,
and add format-specific stub methods so DEX and PYC files are detected.

Changes:
1. Register 26 format-specific global objects as aliases to Binary,
   matching all upstream _init scripts that do `var File = <FORMAT>;`
2. Add generic format stubs: getFileFormatName/Version/Options,
   isVerbose, isDeepScan, isHeuristicScan
3. Create independent DEX and PYC objects via Object.create(Binary)
   to avoid cross-contamination of format-specific methods
4. Add DEX-specific stubs: getMapItemsHash, getOperationSystemName/
   Version/Options, isDexStringPresent, isDexItemStringPresent
5. Add PYC-specific stub: isConstPresent
6. DEX.getFileFormatName returns "Dalvik Executable (.DEX)"
   PYC.getFileFormatName returns "Python bytecode compiled (.PYC)"

Result:
- minimal.dex: detected as "Dalvik Executable (.DEX)"
- minimal.pyc: detected as "Python bytecode compiled (.PYC)"
- minimal.rar: still no detection (corpus file 21 bytes < rule's 64)
- payload.txt.gz: still no detection (corpus timestamp=0, rule rejects)

360 tests pass, clippy clean, check-deps OK.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
