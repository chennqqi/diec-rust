# diec-rust 项目协作指南

## 项目目标

本项目使用 Rust 重新实现 [horsicq/DIE-engine](https://github.com/horsicq/DIE-engine)，目标是提供更清晰的架构、更少的运行时依赖、更好的性能和可移植性。

当前交付范围：

- 在检测能力和可观察行为上与上游 DIE-engine 保持一致。
- 上游检测规则按原始内容 1:1 复用；不得擅自改写规则语义。
- 提供命令行程序。
- 提供稳定的 C ABI，并可构建静态库 `.a`，供 C、Go、Python 等语言调用。
- GUI 不属于当前实现范围，只能记录在 `ROADMAP.md` 的未来计划中。
- 使用自动化测试和上游差分测试证明兼容性。

“Rust 重写”不表示逐行翻译 C++。兼容的是能力、规则语义、输入输出和边界行为；内部设计应遵循 Rust 的安全性与模块化惯例，不延续上游与 Qt 强耦合的结构。

## 事实来源与兼容基线

- 上游仓库：`https://github.com/horsicq/DIE-engine`
- 每次导入或更新上游内容时，必须记录确切的上游 commit SHA。
- 规则文件必须保持可追溯：记录来源路径、commit SHA、文件哈希和本地同步时间。
- 不使用“最新版”作为测试基线；差分测试必须固定到具体的上游版本。
- 上游规则更新应作为独立变更提交，避免与引擎代码修改混在一起。
- 若 Rust 实现与上游表现不同，默认视为兼容性缺陷。只有确认是上游缺陷且项目明确决定修正时，才允许偏离，并必须记录理由及回归用例。
- 复制或分发上游代码、规则、测试样本时，保留原始许可证与归属信息。DIE-engine 当前采用 MIT 许可证，但引入每个子模块、规则集或样本前仍需单独核对其来源和许可。

## 建议架构

仓库应采用 Cargo workspace，将公共能力与适配层解耦。推荐边界如下，实际 crate 名称可在首次脚手架设计时确定：

- `core`：扫描编排、公共数据模型、错误类型和稳定的纯 Rust API。
- `formats`：PE、ELF、Mach-O、DEX、archive 等格式解析；各格式彼此隔离。
- `rules`：上游规则加载、解析、编译和执行；原始规则作为独立资源管理。
- `ffi`：只包含 C ABI、句柄生命周期、内存所有权和错误码映射。
- `cli`：纯 Rust API 的薄封装，不承载检测逻辑。
- `xtask` 或 `tools`：规则同步、基线生成、差分测试和发布辅助工具。

依赖方向必须保持单向：`cli` 和 `ffi` 依赖核心层，核心层不得反向依赖 CLI、FFI 或任何 GUI 框架。解析器不得依赖 Qt。优先选用纯 Rust、维护活跃且跨平台的依赖；增加大型依赖、C/C++ 依赖或系统库前必须说明必要性和跨平台影响。

## 实现原则

- 默认禁止 `unsafe`。确有必要时，将其限制在最小模块内，写明安全不变量，并添加针对边界条件的测试。
- 对不可信二进制输入进行防御性处理：所有偏移、长度、整数运算和切片访问都必须检查。
- 禁止因畸形输入而 panic、越界读取、无限循环或无界分配。
- 核心 API 应支持从字节切片扫描；文件路径扫描只是其上层适配。
- 检测结果使用结构化数据模型，CLI 文本/JSON 和 FFI 序列化都由同一模型生成。
- 结果顺序必须确定且可复现。若并行扫描改变可见顺序，应在输出前稳定排序。
- 性能优化必须基于 benchmark 或 profiling；不得以破坏兼容性、可读性或安全性换取未经测量的性能。
- 不在核心库中引入日志后端或全局运行时。通过调用方可配置的接口暴露诊断信息。

## 规则管理

- 原始上游规则作为 vendored 资源保存，不进行格式化、自动修复或手工编辑。
- 若运行时需要编译后的规则，应由可复现工具从原始规则生成；生成物不得替代原始规则成为事实来源。
- 同步工具应能够检测本地修改、遗漏文件和哈希不匹配。
- 规则解析器要覆盖上游实际语法，不通过静默忽略未知字段来制造“兼容”。
- 无法支持的规则必须产生明确诊断，并计入兼容性测试失败。

## C ABI 与静态库

- FFI 导出使用 `extern "C"` 和明确的 `#[no_mangle]`/等效稳定导出方式。
- ABI 中不得出现 Rust 专有类型，如 `String`、`Vec`、trait object、引用、Rust enum 或未固定布局的结构体。
- 公共结构使用固定宽度整数、`#[repr(C)]` 和不透明句柄。
- 每个由库分配的对象或字符串都必须提供对应释放函数；头文件中明确所有权和线程安全约束。
- panic 不得跨越 FFI 边界。导出函数必须捕获 panic，并转换为稳定错误码。
- ABI 版本必须可查询。破坏 ABI 的变更需要显式版本策略和兼容性说明。
- 发布验证至少覆盖 C 链接 smoke test；Go/cgo 和 Python ctypes/cffi 各提供一个最小集成测试或示例。
- Unix-like 目标生成 `.a`；Windows 同时考虑静态 `.lib`，但不得让平台差异污染核心 API。

## CLI 约定

- CLI 是库能力的完整可验证入口，但不复制核心逻辑。
- 为自动化调用提供稳定的 JSON 输出模式；人类可读输出可以迭代，但兼容字段必须测试。
- 退出码应区分：成功完成、发现结果、输入/参数错误、规则错误和内部错误。
- 大文件扫描尽量流式或使用受控映射，避免无意义的整文件复制。
- 当前禁止加入 GUI toolkit、GUI crate 或 GUI 构建目标。

## 测试与兼容性要求

每项功能都必须同时考虑以下层次：

- 单元测试：解析器、规则语法、边界运算和错误映射。
- 集成测试：从样本输入到结构化扫描结果。
- 差分测试：同一语料、同一规则版本分别运行上游 DIE-engine 和本项目，比较规范化后的结果。
- FFI 测试：ABI、内存生命周期、空指针、无效 UTF-8、panic 隔离和多次调用。
- 模糊测试：所有处理不可信字节的解析入口。
- 性能测试：具有代表性的文件类型、文件大小和规则规模，记录时间与峰值内存。
- 跨平台测试：至少 Linux、Windows、macOS；支持的 CPU 架构按发布矩阵逐步扩展。

差分比较必须保留原始输出和规范化输出。规范化只能消除无语义差异，例如路径表示或明确不保证的耗时字段；不得隐藏漏报、误报、检测名称、版本、类型、顺序或层级差异。每个已知差异都应有带原因的 allowlist 条目，禁止使用宽泛匹配跳过失败。

测试语料应包含：

- 上游公开测试/示例文件（许可允许时）。
- 各支持格式的最小有效文件。
- 常见 packer、compiler、linker、installer、protector 和 archive 样本。
- 截断、损坏、重叠区间、整数溢出诱因和随机数据。
- 历史回归样本。

恶意或来源不明的样本不得直接提交到仓库；优先使用可复现生成器、哈希清单或隔离的外部语料库，并记录许可与安全处理方式。

## 完成标准

变更只有在以下条件满足时才算完成：

- 实现位于正确的架构层，没有引入反向依赖。
- 新行为有测试；修复缺陷时先提供或同时提供回归测试。
- `cargo fmt --check`、`cargo clippy --workspace --all-targets --all-features -- -D warnings` 和 `cargo test --workspace --all-features` 通过。
- 涉及输入解析的变更已考虑 fuzz corpus 或属性测试。
- 涉及兼容行为的变更已运行对应的固定版本上游差分测试。
- 涉及 ABI 的变更已运行 C 链接与生命周期测试，并同步公共头文件。
- 文档、兼容矩阵和上游基线记录已更新。

在 workspace 尚未创建前，上述 Cargo 命令是目标约定，而不是当前可运行命令。首次脚手架变更应建立 CI，并把实际命令固化到仓库文档中。

## 提交与评审

- 保持变更小而聚焦；规则同步、格式解析器、FFI 和 CLI 改动尽量分别提交。
- 不提交构建产物、临时扫描输出、私有样本或本机路径。
- PR/变更说明应列出：实现能力、上游对应位置或行为、测试证据、已知差异、性能/依赖影响。
- 对兼容性存在疑问时，先用固定输入观察上游行为，再实现和测试；不要凭文件格式常识猜测 DIE-engine 的具体语义。

