phase5: add Go/cgo and Python bindings, FFI sanitizer tests

Complete Phase 5 language integration with bindings and edge-case tests.

Changes:
1. Add cdylib to diec-ffi crate-type for dynamic linking:
   - Now builds diec_ffi.lib (staticlib) + diec_ffi.dll (cdylib)
   - Python ctypes and Go cgo can link against the DLL

2. Go/cgo bindings (bindings/go/diec/):
   - diec.go: Database, Scanner, Result types with Close()
   - One-shot ScanBytes/ScanPath functions
   - ABI version and compatibility helpers
   - Error handling via DiecError Go error
   - diec_test.go: 5 tests (ABI version, compatibility, scan bytes,
     scan path, error handling) - all pass with mingw gcc

3. Python ctypes bindings (bindings/python/diec.py):
   - Database, Result classes with context manager support
   - scan_bytes/scan_path one-shot functions
   - ScanOptions struct matching C layout
   - Automatic library discovery (DIEC_LIB_PATH or relative paths)
   - test_diec.py: 9 tests (ABI version, status names, scan bytes,
     flags, context manager, error handling) - all pass

4. FFI sanitizer tests (crates/diec-ffi/tests/ffi_sanitizer.rs):
   - 16 tests covering edge cases:
     - Double-free safety for all handle types (builder, database,
       cancel, result, scanner, error)
     - Null pointer validation for all accessors
     - Null database to scan returns INVALID_ARGUMENT
     - Null data with non-zero length returns INVALID_ARGUMENT
     - Null data with zero length is valid (empty input)
     - Error handle query and free lifecycle
     - Cancel request on null returns INVALID_ARGUMENT
     - Status name lookup for all 16 codes
     - Scan options init with null and small size

5. Mark FFI functions as `unsafe extern "C"` for Rust 2024 safety

6. Update ROADMAP.md: Phase 5 marked as complete

Result:
- 411 tests pass (374 existing + 37 FFI), clippy clean
- Go, Python, and C can all use the C ABI
- All handle types verified for double-free safety

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
