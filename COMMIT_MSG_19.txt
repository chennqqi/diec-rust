phase3: upstream-compatible globals, endian support, real rule tests

Sixth step of Phase 3 (rule compatibility runtime). Update global
functions to match upstream die_global_script.h signatures, add
endian-aware read methods, and test with real upstream rule files.

Global function signature updates:
- _isResultPresent(sType, sName) -> bool: 2-arg case-insensitive match
- _getNumberOfResults(sType) -> int: 1-arg counts results of given type
- _removeResult(sType, sName) -> void: 2-arg removes first match and
  adds to block list so same type+name cannot be re-added
- _setResult: checks block list before adding
- _getQtVersion() -> "5.15.13"
- Endian constants _LE=0, _BE=1

Binary host API bridge (15 new methods):
- readSWord, readSDword, readSQword (signed reads)
- read_uint8/int8/uint16/int16/uint24/uint32/int32/uint64/int64
  with endian parameter support
- isVerbose()

New test module tests/real_rules.rs (3 tests):
- Loads real upstream _init framework script + _debug,
  _runtime_helpers, language include scripts
- 7z signature detection: archive_7z.1.sg correctly detects 7-Zip
- 7z no match: random data produces no false positive
- ZIP signature detection: archive_ZIP.1.sg runs without crash

cargo fmt/clippy/test/check-deps all pass (331 tests total:
44 diec-core + 98 diec-rules lib + 22 conformance + 3 real_rules +
156 diec-formats + 5 corpus + 3 other).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
