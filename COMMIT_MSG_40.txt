phase4: add CLI scan flags matching upstream (--heuristicscan, --deepscan, etc.)

Add scan mode flags to the CLI that match upstream DIE-engine console
options. These flags control rule behavior through the host API.

Changes:
1. Add ScanFlags struct to diec-engine::host with fields:
   - deep, heuristic, verbose, aggressive, all_types, hide_unknown
2. Add is_verbose() to HostApi trait (already had is_deep/is_heuristic/
   is_aggressive/is_recursive).
3. Update BufferHost to store ScanFlags and implement is_verbose().
4. Update scan_once/scan_bytes to accept ScanFlags parameter.
5. Update CLI with new flags:
   --deepscan, --heuristicscan, --verbose, --aggressivescan,
   --alltypes, --hideunknown
6. Fix isVerbose/isDeepScan/isHeuristicScan in host_api_bridge:
   removed duplicate hardcoded false definitions that were overriding
   the host-backed implementations.
7. Update all test hosts to implement is_verbose().
8. Update ROADMAP.md and AGENTS.md to reflect Phase 4 status.

Result:
- CLI now supports all upstream scan mode flags
- Flags propagate through BufferHost to rule runtime
- 361 tests pass, clippy clean

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
