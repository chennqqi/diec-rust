phase4: add multi-database support, --showdatabase, --showstructs

Add remaining CLI database and information query features to match
upstream DIE-engine console capabilities.

Changes:
1. DatabaseBuilder now supports multiple database directories via
   with_extra() method. Rules from all directories are merged.
2. --extradb <path>: add extra database directory (db_extra).
3. --customdb <path>: add custom database directory (db_custom).
4. --showdatabase: print database paths and per-file-type rule counts.
5. --showstructs: list available struct methods (static list).
6. Both --showdatabase and --showstructs work without input files.
7. Add Database::rules() accessor method.
8. Add 3 new CLI integration tests: cli_showdatabase, cli_showstructs,
   cli_extradb_flag.

Result:
- CLI now supports 24/25 upstream capabilities
- 24 CLI integration tests, 374 total tests pass, clippy clean

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
