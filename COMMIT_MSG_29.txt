phase4: implement 20+ missing host API functions

Seventh step of Phase 4 (CLI). Implement commonly used host API
functions that were missing, causing "TypeError: not a function"
exceptions in many rules.

New host API functions (by usage frequency):
- isVerbose() -> false (no verbose mode in CLI)
- readByte(offset) -> u8 or -1 on out-of-bounds
- findString(offset, size, pattern) -> byte search
- isDeepScan() -> false
- getOverlayOffset() -> -1 (no overlay)
- readWord(offset) -> u16 LE
- isHeuristicScan() -> false
- isOverlay() -> false
- readDword(offset) -> u32 LE
- isResource() -> false
- cleanString(s) -> no-op
- read_ansiString(offset, maxSize) -> ANSI string (stops at null)
- read_unicodeString(offset, maxSize) -> UTF-16LE string
- findByte(offset, size, byte) -> byte search
- bytesCountToString(n) -> human-readable size
- isPlainText() -> false
- isText() -> false
- isZeroFilled(offset, size) -> false
- isDebugData() -> false
- getScanID() -> empty string
- getFileSuffix() -> empty string
- getHeaderString() -> empty string

Bug fixes:
- readByte: return -1 on out-of-bounds (was returning 0)
- getFileBaseName: removed duplicate registration that overrode
  the correct implementation

Corpus scan improvement:
- PDF: "format: PDF (1.4) [binary data]"

cargo fmt/clippy/test/check-deps all pass (356 tests total).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
