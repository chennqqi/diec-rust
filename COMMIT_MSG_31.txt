phase4: more host API functions + X shortcuts + findSignature fix

Ninth step of Phase 4 (CLI). Add more missing host API functions,
X shortcuts, and fix findSignature to support both 2-arg and 3-arg
forms. This eliminates all but one diagnostic error.

New host API functions:
- crc16(offset, size) -> CRC-16 (Modbus variant)
- readBytes(offset, size, replaceZeroWithSpace?) -> byte array
  (native 2-arg + JS wrapper for optional 3rd arg)
- fSig(offset, size, signature) -> findSignature alias
- find_utf8String(offset, maxSize) -> UTF-8 string
- read_codePageString(offset, maxSize, codePage?) -> code page string
- read_ucsdString(offset) -> Pascal-style length-prefixed string
- I16/I24/I64(offset) -> signed integer reads (LE + endianness wrappers)

Add Util.div64 alias (used by charStat in "read" include script).

Add X shortcuts (JS wrappers):
- X.fStr = File.findString
- X.BA = File.readBytes
- X.SA = File.read_ansiString
- X.SC = File.read_codePageString
- X.SU8 = File.read_utf8String
- X.SU16 = File.read_unicodeString
- X.UCSD = File.read_ucsdString
- X.F16/F32/F64 -> float stubs (return 0.0)

Fix findSignature to support both 2-arg (start, sig) and 3-arg
(start, size, sig) forms via JS wrapper.

Add I16/I24/I64 to endianness wrappers.

Result: only 1 diagnostic error remains (Nintendo-certified-file.1.sg
has a const redeclaration that QuickJS rejects but QtScript allowed;
this is an upstream rule bug).

cargo fmt/clippy/test/check-deps all pass (358 tests total).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
