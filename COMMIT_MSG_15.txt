phase3: integrate rquickjs backend and global host functions

Second step of Phase 3 (rule compatibility runtime). Integrate
rquickjs@=0.12.1 (vendored QuickJS-NG) as the first production
rule runtime backend per ADR 0006.

diec-rules/Cargo.toml:
- Pin rquickjs to =0.12.1 with default-features=false, features=["std"]
- All rquickjs/QuickJS types are private to backend_rquickjs module

New module backend_rquickjs.rs (16 unit tests):

- RquickjsRuntimeFactory: implements RuleRuntimeFactory
- RquickjsRuntime: implements RuleRuntime
  - Runtime::new() with set_memory_limit, set_max_stack_size,
    set_interrupt_handler (cooperative cancellation via CancelFlag)
  - Context::full() for complete intrinsics
  - register_globals(): 15 global host functions + meta() defined
    via JavaScript eval to avoid Function::new lifetime issues
  - Results collected via JS __diec_results array, read back through
    read_results()/clear_results() after detect() evaluation
  - load_database/init/evaluate_rule/shutdown implementation
  - Sloppy mode eval to match Qt Script behavior

RuleRuntime trait change:
- Remove Send bound (ADR 0006: single worker thread owns runtime,
  cross-thread communication uses only cancel/deadline tokens)

Global functions registered:
- _setResult, _setLang, _error, _log, _getEngineVersion,
  _isStop, _isConsoleMode, _isLiteMode, _isGuiMode,
  _isLibraryMode, _getOS, _getNumberOfResults, _isResultPresent,
  _breakScan, _encodingList, _removeResult, meta

cargo fmt/clippy/test/check-deps/doc all pass (284 tests total:
44 diec-core + 76 diec-rules + 156 diec-formats + 5 corpus + 3 other).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
