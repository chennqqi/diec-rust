phase5: implement stable C ABI with opaque handles and panic containment

Implement the complete C ABI for diec-rust, providing a stable
interface for C, Go/cgo, and Python ctypes/cffi consumers.

Changes:
1. Create public header include/diec.h with:
   - ABI version negotiation (diec_abi_version, diec_abi_is_compatible)
   - 16 status codes (OK through ALLOCATION_FAILED)
   - Opaque handle types (database_builder, database, scanner,
     cancel, result, error)
   - Scan options struct with additive extension via struct_size
   - Database builder, one-shot scan, reusable scanner APIs
   - Result and error accessors with paired free

2. Implement diec-ffi crate with modules:
   - status.rs: DiecStatus enum with from_u32 and name lookup
   - panic.rs: catch_panics helper using catch_unwind
   - handles.rs: opaque handle types wrapping Rust types
   - error.rs: pointer validation, ffi_wrap/ffi_wrap_out helpers
   - scan.rs: all #[no_mangle] extern "C" exported functions

3. Safety features:
   - All FFI functions catch panics (no unwinding across boundary)
   - Pointer-to-pointer free with null check (double-free safe)
   - NULL pointer validation on all inputs
   - UTF-8 validation for path inputs
   - NUL byte rejection in paths

4. Add 19 FFI tests (7 unit + 12 integration):
   - ABI version compatibility checks
   - Status name lookup
   - Database builder full lifecycle
   - Cancel token lifecycle
   - One-shot scan_bytes full lifecycle (build DB, scan, verify JSON)
   - Reusable scanner lifecycle
   - Error handle lifecycle
   - Scan options init
   - Null pointer validation

5. Add C smoke test (tests/c/smoke.c) demonstrating complete
   scan lifecycle from C.

6. Update ROADMAP.md and AGENTS.md to Phase 5.

Result:
- 395 tests pass (374 existing + 21 FFI), clippy clean
- C ABI provides stable interface for language bindings
- Panic containment ensures no UB from Rust panics

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
