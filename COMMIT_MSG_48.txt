phase6: expand fuzz targets and add compatibility report

Extend Phase 6 with comprehensive fuzz coverage and release documentation.

Changes:
1. New fuzz targets (3 new, 6 total):
   - fuzz_scan_engine: full scan engine with default/heuristic/all_types
     flags. Verifies no panic, no hang, detection fields non-empty.
     Caches database via OnceLock for fast iteration.
   - fuzz_output_render: all output renderers (JSON, text, XML, CSV, TSV)
     on fuzz-constructed ScanResults. Verifies no panic and JSON is
     re-parseable. Tests empty results, non-UTF-8 paths, and multiple
     detections.
   - fuzz_scan_ffi: C ABI scan_bytes via FFI. Verifies no panic crosses
     the boundary, status codes are valid (0-15), and double-free on
     null handles is safe. Uses SendPtr wrapper for thread-safe handle
     caching.

2. Update fuzz/Cargo.toml: add diec-engine, diec-output, serde_json
   dependencies and 3 new [[bin]] entries.

3. Compatibility report (COMPATIBILITY.md):
   - Rule loading: 1184/1186 = 99.83% success rate
   - Corpus differential: 27/27 matched, 0 mismatches
   - Edge-case robustness: 20/20 no crash, 7/7 no spurious, 20/20 no hang
   - CLI feature compatibility: 21 features verified
   - C ABI compatibility: 12 features verified
   - Performance baseline: scan ~117ms, db load ~1s, all_types ~320ms
   - Test summary: 414 tests, 6 fuzz targets, 0 failures

4. Update docs/design/testing.md: document all 6 implemented fuzz
   targets with coverage layer and invariants.

5. Update ROADMAP.md and requirements.md for Phase 6 progress.

Result:
- 414 tests pass, clippy clean
- 6 fuzz targets cover core/formats/engine/output/ffi layers
- Compatibility report provides release-ready documentation

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
