phase3: Archive object, end-to-end detection tests, PE batch loading

Eighth step of Phase 3 (rule compatibility runtime). Confirm Archive
object works via JS include script, add end-to-end detection tests,
and verify PE rule batch loading.

Archive object: confirmed that `Archive` is a pure JS object defined
in the `archive-file` include script (Archive.add/contents methods).
No Rust-side native implementation needed — rules that
includeScript("archive-file") get Archive automatically.

New end-to-end detection tests (real_rules.rs, 3 new tests):
- real_rule_ar_detects_signature: AR archive format detection
  (uses Archive.add/Archive.contents from archive-file include)
- real_rule_bzip_detects_signature: BZip2 format detection
- real_rule_gzip_detects_signature: GZIP format detection

New test module tests/batch_load_pe.rs:
- Loads all 834 PE rules from upstream Detect-It-Easy db
- 826/834 succeed (99.0%)
- 8 failures: rules using PE object at top level (PE is not defined),
  requiring PE-specific host API (not yet implemented)

Total test count: 336 (44 diec-core + 98 diec-rules lib + 22
conformance + 6 real_rules + 1 batch_load + 1 batch_load_pe + 156
diec-formats + 5 corpus + 3 other).

cargo fmt/clippy/test/check-deps all pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
