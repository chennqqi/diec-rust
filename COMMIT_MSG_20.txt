phase3: batch rule loading 99.7% compatibility, sloppy mode fix

Seventh step of Phase 3 (rule compatibility runtime). Fix QuickJS
strict mode incompatibility, add short aliases, and verify batch
loading of all 292 Binary rules.

Critical fix: use eval_with_options(strict: false) to match QtScript
sloppy mode behavior. Without this, rules using `delete` operator
fail with "cannot delete a direct reference in strict mode".

Fix getString(offset) single-arg call: register JS wrapper that
defaults maxLen to 0 (read to end of file) when omitted.

Binary host API additions (22 new methods):
- Short aliases: Sz, c, SA, SC, fStr, fSig, BA
- Signed reads: readSWord, readSDword, readSQword
- Endian-aware: read_uint8/int8/uint16/int16/uint24/uint32/int32/
  uint64/int64 with _LE/_BE endian parameter
- isVerbose()

Runtime improvements:
- Type init scripts deferred to init() phase (after host API
  registration) so Binary/_init can set X=Binary and includeScript(
  "read")
- Directory-form include scripts (db/<dir>/<dir> file)
- Improved exception message extraction via ctx.catch() + String()
  eval, revealing SyntaxError/TypeError details

New test module tests/batch_load.rs:
- Loads all 292 Binary rules from upstream Detect-It-Easy db
- 291/292 succeed (99.7%)
- Only failure: format_bin.Nintendo-certified-file.1.sg has upstream
  rule bug (const tp redeclares var tp, QuickJS correctly rejects)

cargo fmt/clippy/test/check-deps all pass (334 tests total).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
