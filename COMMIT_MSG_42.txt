phase4: add --format, --profiling, --messages CLI options

Add three remaining CLI scan control options to match upstream
DIE-engine console capabilities.

Changes:
1. --format: format the output result with extra spacing around
   type separators and version/option brackets. Implemented as
   render_text_formatted() in diec-output.
2. --profiling: print scan timing information to stderr after
   scan completes (file count and elapsed time).
3. --messages: print scan diagnostics (errors/warnings) to stderr.
4. Add 3 new CLI integration tests: cli_format_flag,
   cli_profiling_flag, cli_messages_flag.

Result:
- CLI now supports all upstream scan control flags
- 18 CLI integration tests, 371 total tests pass, clippy clean

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
