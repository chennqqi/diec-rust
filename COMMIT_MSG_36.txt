phase4: implement Mach-O host API with real Mach-O parsing

Thirteenth step of Phase 4 (CLI). Replace Mach-O stub methods with
real implementations that parse the Mach-O header and load commands
from raw bytes.

Changes:
1. Implement 25+ Mach-O host API methods in JavaScript using Binary
   read primitives:
   - Header: is64, getType, getMachine, getEntryPoint (from LC_MAIN)
   - Sections: getNumberOfSections, getSectionName/Number/FileOffset/
     FileSize, isSectionNamePresent (parsed from LC_SEGMENT commands)
   - Segments: getNumberOfSegments
   - Libraries: getNumberOfLibraries, isLibraryPresent,
     isLibraryNamePresent, getLibraryCurrentVersion (from LC_LOAD_DYLIB)
   - Metadata: getGeneralOptions, getOperationSystemName
   - Search: findSignature (2/3-arg), findString (2/3-arg), getString
   - Entry point: compareEP (uses LC_MAIN entryoff)
2. Fix JS syntax error: object literal keys cannot be expressions
   (7 + 0x01000000: "x86_64" is invalid), use bracket assignment
3. Use Binary.* directly instead of File.* to avoid infinite recursion

Result:
- Mach-O rules no longer throw "Exception generated by QuickJS"
- Minimal Mach-O files have no detections (expected: no compiler/
  packer signatures in minimal files)
- minimal-fat.macho still correctly detected as "converter: lipo"
- All existing detections preserved

360 tests pass, clippy clean, check-deps OK.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
