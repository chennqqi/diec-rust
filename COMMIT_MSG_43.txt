phase4: add --entropy and --info special modes

Add two special scan modes that bypass rule detection and output
file metadata directly.

Changes:
1. --entropy: compute Shannon entropy of the file (0.0-8.0) and
   display with file size. Supports text and JSON output.
2. --info: display file size information. Supports text and JSON.
3. Both modes skip rule database loading entirely.
4. Add 3 new CLI integration tests: cli_entropy_mode,
   cli_entropy_json_mode, cli_info_mode.

Result:
- CLI now supports all upstream special modes
- 21 CLI integration tests, 374 total tests pass, clippy clean

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
