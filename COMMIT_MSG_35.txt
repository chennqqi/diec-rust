phase4: implement ELF host API with real ELF parsing

Twelfth step of Phase 4 (CLI). Replace ELF stub methods with real
implementations that parse the ELF header and section/program header
tables from raw bytes.

Changes:
1. Create ELF, MACH, MACHOFAT as independent objects (Object.create)
   with Binary properties copied in, avoiding cross-contamination
2. Implement 30+ ELF host API methods in JavaScript using Binary
   read primitives:
   - Header: is64, getElfHeader_entry/type/machine/shnum/shstrndx/
     phnum/phoff/shoff/ehsize/phentsize/shentsize
   - Sections: getNumberOfSections, getSectionName/Number/FileOffset/
     FileSize, isSectionNamePresent
   - Programs: getNumberOfPrograms, getProgramFileOffset/FileSize
   - Dynamic: isLibraryPresent (parses DT_NEEDED), getDynamicTableOffset
   - Strings: isStringInTablePresent, getString
   - Entry point: getEntryPoint, compareEP (vaddr→file offset conversion)
   - Metadata: getType, getMachine, getGeneralOptions,
     getOperationSystemName (from EI_OSABI)
   - Search: findSignature (2/3-arg), findString (2/3-arg)
3. Use Binary.* directly instead of File.* in ELF helpers, because
   _init sets File=ELF which would cause infinite recursion

Result:
- ELF rules no longer throw RangeError (stack overflow) or TypeError
- Minimal ELF files have no detections (expected: no compiler/packer
  signatures in minimal files)
- All existing detections preserved

360 tests pass, clippy clean, check-deps OK.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
